{
  "name": "detbench",
  "version": "0.1.0",
  "description": "AI Determinism Benchmark — context-layer determinism (rebuild-identity, query-stability, drift-under-noise, cross-machine identity) with Determinism Levels D0-D4 and a verifiable fingerprint format",
  "type": "module",
  "bin": { "detbench": "./detbench.mjs" },
  "engines": { "node": ">=20" },
  "scripts": {
    "bench": "node detbench.mjs run --adapters bm25",
    "bench:all": "node detbench.mjs run --adapters bm25,emb-minilm,spiderbrain"
  },
  "optionalDependencies": {
    "@xenova/transformers": "^2.17.2"
  },
  "keywords": ["determinism", "reproducibility", "benchmark", "rag", "retrieval", "agent-memory", "context-engineering"],
  "license": "MIT"
}
